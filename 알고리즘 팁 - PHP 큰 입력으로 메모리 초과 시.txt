
입력이 큰 문제의 경우, 단순 fgets로 전부 읽으면 메모리 초과가 날 수 있다.

이런 경우 

while(($buffer = fgets(STDIN, 4096)) !== false){
    $text .= $buffer;
}

위와 같은 식으로 나눠서 받는 것이 한 번에 큰 메모리를 쓰는 것을 방지할 수 있다.

너무 버퍼를 작게 하면 시간이 더 오래 걸리고,

버퍼를 너무 크게 하면 순간적으로 큰 메모리를 소모하여 메모리 초과가 나게 된다.